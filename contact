<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>Contact — Aarambh</title>

  <!-- Google Fonts (same as index.html) -->
  <link href="https://fonts.googleapis.com/css2?family=Montserrat+Alternates:wght@300;400;600;700&family=Pompiere&family=Cinzel+Decorative:wght@400;700&display=swap" rel="stylesheet">

  <style>
    /* Branding variables (same as index.html) */
    :root{
      --accent:#533A5A;
      --muted:#EEB885;
      --bg:#FFFFFF;
      --card:#EADFC8;
      --text:#533A5A;
      --glass: rgba(255,255,255,0.6);
    }

    *{box-sizing:border-box}
    html,body{
      margin:0;
      font-family:'Montserrat Alternates', sans-serif;
      background:var(--bg);
      color:var(--text);
      -webkit-font-smoothing:antialiased;
      -moz-osx-font-smoothing:grayscale;
      line-height:1.45;
    }

    /* Headings (match index) */
    h1,h2,h3,.hero-title{ font-family:'Cinzel Decorative', serif; color:var(--accent); margin:0; }

    .secondary{ font-family:'Pompiere', cursive; letter-spacing:0.4px; color:var(--muted); }

    /* Header (identical to index.html) */
    header{
      display:flex;
      justify-content:space-between;
      align-items:center;
      padding:18px 28px;
      background:var(--accent);
      color:var(--bg);
      position:sticky;
      top:0;
      z-index:50;
      box-shadow:0 6px 24px rgba(83,58,90,0.08);
    }
    .brand{display:flex;align-items:center;gap:14px}
    .brand img{height:56px;display:block}
    .brand h1{font-size:18px;margin:0;color:var(--bg);letter-spacing:0.6px;font-family:'Cinzel Decorative', serif !important;}
    nav{display:flex;gap:20px;align-items:center}
    nav a{color:var(--bg);text-decoration:none;font-weight:600;padding:6px 8px;border-radius:8px;font-family:inherit}
    nav a:hover{background:var(--glass);color:var(--bg)}

    .btn-primary{
      background:var(--accent);
      color:var(--bg);
      padding:11px 18px;
      border-radius:12px;
      font-weight:800;
      text-decoration:none;
      display:inline-flex;
      align-items:center;
      gap:8px;
      box-shadow:0 6px 18px rgba(83,58,90,0.12);
      transition:transform .12s ease,box-shadow .12s ease,opacity .12s ease;
      font-family: 'Montserrat Alternates', sans-serif;
    }
    .btn-primary:hover{transform:translateY(-2px);box-shadow:0 10px 24px rgba(83,58,90,0.16);opacity:0.98}

    .btn-secondary{
      background:transparent;
      color:var(--accent);
      padding:10px 16px;
      border-radius:12px;
      font-weight:700;
      text-decoration:none;
      border:2px solid transparent;
      transition:background .15s ease,color .15s ease,border-color .15s ease;
      font-family: 'Montserrat Alternates', sans-serif;
    }
    .btn-secondary:hover{background:var(--muted);color:var(--text);border-color:var(--accent)}

    .nav-toggle{display:none;background:transparent;border:0;color:var(--bg);font-size:20px;padding:8px;border-radius:8px}

    /* Page container */
    .wrap{max-width:1100px;margin:44px auto;padding:0 20px}

    /* Page title */
    .page-hero{
      display:flex;
      justify-content:space-between;
      align-items:center;
      gap:20px;
      margin-bottom:24px;
      flex-wrap:wrap;
    }
    .page-hero .title{
      font-family:'Cinzel Decorative', serif;
      font-size:32px;
      color:var(--accent);
    }
    .page-hero .subtitle{
      font-family:'Montserrat Alternates',sans-serif;
      color:var(--muted);
      font-weight:700;
    }

    /* Contact card */
    .contact-grid{
      display:grid;
      grid-template-columns:1fr 420px;
      gap:28px;
      align-items:start;
    }

    .contact-form{
      background:var(--card);
      border-radius:14px;
      padding:20px;
      box-shadow:0 10px 30px rgba(83,58,90,0.06);
    }

    label{display:block;font-weight:700;margin-bottom:8px;font-size:14px}
    input[type="text"], input[type="email"], input[type="tel"], textarea{
      width:100%;
      padding:12px;
      border-radius:10px;
      border:1px solid rgba(0,0,0,0.12);
      font-size:15px;
      margin-bottom:12px;
      font-family:'Montserrat Alternates',sans-serif;
    }
    textarea{min-height:120px;resize:vertical}

    .form-actions{display:flex;gap:12px;justify-content:flex-end;align-items:center}
    .btn{padding:10px 16px;border-radius:12px;border:none;cursor:pointer;font-weight:800}
    .btn--primary{background:var(--accent);color:var(--bg)}
    .btn--ghost{background:transparent;border:2px solid var(--accent);color:var(--accent)}

    /* Sidebar contact info */
    .contact-side{
      background:var(--bg);
      border-radius:14px;
      padding:18px;
      box-shadow:0 8px 24px rgba(83,58,90,0.04);
      border:1px solid rgba(83,58,90,0.04);
    }
    .contact-side h3{font-family:'Cinzel Decorative', serif;margin:0 0 8px 0;color:var(--accent)}
    .contact-item{display:flex;gap:12px;align-items:flex-start;margin-bottom:12px}
    .contact-item .meta{font-weight:700;color:var(--text)}
    .small{font-size:13px;color:var(--muted);font-weight:600}

    .map-placeholder{
      width:100%;
      height:180px;
      border-radius:10px;
      background:#d8cbb7;
      display:flex;
      align-items:center;
      justify-content:center;
      color:var(--accent);
      font-weight:700;
      margin-top:8px;
    }

    footer{margin-top:40px;padding:26px 20px;text-align:center;color:var(--muted)}

    /* Responsive */
    @media (max-width:980px){
      .contact-grid{grid-template-columns:1fr}
    }
    @media (max-width:720px){
      nav{position:fixed;right:0;top:64px;background:var(--accent);width:260px;height:calc(100% - 64px);flex-direction:column;align-items:flex-start;padding:20px;gap:14px;transform:translateX(100%);transition:transform .25s ease;z-index:60}
      nav.open{transform:translateX(0)}
      nav a{width:100%;padding:10px 12px;border-radius:8px}
      .nav-toggle{display:inline-flex}
      header{padding:12px 16px}
    }
  </style>
</head>
<body>

  <!-- header (identical to home) -->
  <header>
    <div class="brand">
      <img src="AarambhLogo2.png" alt="Aarambh logo">
      <div>
        <h1 style="font-family:'Cinzel Decorative', serif !important;margin:0;">Aarambh</h1>
        <div style="font-size:12px;color:var(--muted);font-weight:700;font-family:'Pompiere',cursive !important">Where every story begins</div>
      </div>
    </div>

    <button class="nav-toggle" aria-label="Open menu" aria-expanded="false">☰</button>

    <nav aria-label="Primary navigation">
      <a href="index.html">Home</a>
      <a href="team.html" target="_blank">Team Aarambh</a>
      <a href="select-seats.html">Book Tickets</a>
      <a class="btn-secondary" href="contact.html" style="background:var(--bg);color:var(--accent);border-color:var(--bg);">Contact Us</a>
    </nav>
  </header>

  <main class="wrap" role="main">
    <div class="page-hero">
      <div>
        <div class="title">Contact Us</div>
        <div class="subtitle">Questions, partnerships or tickets — reach out to Team Aarambh.</div>
      </div>
    </div>

    <div class="contact-grid" aria-live="polite">
      <!-- FORM -->
      <section class="contact-form" aria-labelledby="contact-form-title">
        <h2 id="contact-form-title" style="font-size:20px;margin-bottom:12px;font-family:'Cinzel Decorative',serif;color:var(--accent);">Send us a message</h2>

        <form id="contactForm" onsubmit="handleSubmit(event)" novalidate>
          <label for="name">Full name</label>
          <input id="name" name="name" type="text" placeholder="Prathik Kundar" required>

          <label for="phone">Phone / WhatsApp</label>
          <input id="phone" name="phone" type="tel" placeholder="+91 77000 46566" required>

          <label for="email">Email (optional)</label>
          <input id="email" name="email" type="email" placeholder="tickets.aarambh@gmail.com">

          <label for="message">Message</label>
          <textarea id="message" name="message" placeholder="How can we help you?" required></textarea>

          <div class="form-actions" style="margin-top:8px;">
            <button type="button" class="btn btn--ghost" onclick="window.location.href='index.html'">Cancel</button>
            <button type="submit" class="btn btn--primary">Send Message</button>
          </div>

          <p id="formStatus" style="margin-top:12px;font-weight:700;color:var(--accent);display:none"></p>
        </form>
      </section>

      <!-- SIDEBAR -->
      <aside class="contact-side" aria-label="Contact information">
        <h3>Get in touch</h3>

        <div class="contact-item">
          <div class="meta">Phone</div>
          <div><a href="tel:+917700046566" style="color:var(--accent);text-decoration:none;font-weight:700">+91 77000 46566</a></div>
        </div>

        <div class="contact-item">
          <div class="meta">WhatsApp</div>
          <div class="small"><a href="https://wa.me/917700046566" target="_blank" style="color:var(--accent);text-decoration:none">Chat on WhatsApp</a></div>
        </div>

        <div class="contact-item">
          <div class="meta">Email</div>
          <div class="small"><a href="mailto:tickets.aarambh@gmail.com" style="color:var(--accent);text-decoration:none">tickets.aarambh@gmail.com</a></div>
        </div>

        <div style="margin-top:12px">
          <div class="meta">Venue</div>
          <div class="small">Symbiosis College Auditorium</div>
          <div class="map-placeholder" role="img" aria-label="Map placeholder">Map Placeholder</div>
        </div>
      </aside>
    </div>
  </main>

  <footer>
    <div style="max-width:1100px;margin:0 auto;padding:6px 8px;text-align:center">
      © Team Aarambh — All rights reserved
    </div>
  </footer>

  <script>
    // mobile nav toggle (same as index)
    (function(){
      var btn = document.querySelector('.nav-toggle');
      var nav = document.querySelector('header nav');
      if(!btn || !nav) return;
      btn.addEventListener('click', function(){
        var open = nav.classList.toggle('open');
        btn.setAttribute('aria-expanded', open ? 'true' : 'false');
      });
      nav.addEventListener('click', function(e){
        if(e.target.tagName === 'A' && nav.classList.contains('open')){
          nav.classList.remove('open');
          btn.setAttribute('aria-expanded','false');
        }
      });
      window.addEventListener('resize', function(){
        if(window.innerWidth > 720){ nav.classList.remove('open'); btn.setAttribute('aria-expanded','false'); }
      });
    })();

    // Basic front-end submit handling (no backend)
    function handleSubmit(e){
      e.preventDefault();
      var status = document.getElementById('formStatus');
      status.style.display = 'block';
      status.textContent = 'Sending...';

      // simple validation
      var name = document.getElementById('name').value.trim();
      var phone = document.getElementById('phone').value.trim();
      var msg = document.getElementById('message').value.trim();
      if(!name || !phone || !msg){
        status.textContent = 'Please fill required fields.';
        return;
      }

      // simulate async request
      setTimeout(function(){
        status.textContent = 'Thanks — message sent. We will contact you soon.';
        document.getElementById('contactForm').reset();
      }, 900);
    }
  </script>
</body>
</html>
